The following app is a table of students. Tools used were Blazor server, C#, and SQLite.

Here is the basic planning for the app

StudentModel:
- Id: int (primary key)
- Name: string
- Points: int

Library: 
- CRUD operations

BlazorUI:
- appsettings: connectionString to SQLite DB
- GetConnectionString()
